# 3GPP FTP

用途：

- 下载原始规范、会议目录、TDoc ZIP、Meeting Report、agenda、document list。
- 为本地 `TDoc/_incoming/` 提供原始资料。

处理规则：

- 原始 ZIP、DOCX、PDF、TXT 不要删除。
- 转换后的 Markdown、CSV 或抽取结果写入 `TDoc/_processed/`。
- 元数据索引写入 `TDoc/_index/`。
